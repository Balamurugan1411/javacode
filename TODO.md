# Upload Remaining Java Files to GitHub

## Steps (to be checked off as completed):

- [ ] 1. Initialize Git repository (`git init`)
- [ ] 2. Add remote origin (`git remote add origin https://github.com/Balamurugan1411/javacode.git`)
- [ ] 3. Create .gitignore (exclude .class, leetcode/, oops/polymorphism(Day 7)/, IDE files)
- [ ] 4. Create this TODO.md
- [ ] 5. Create branch `blackboxai/upload-files` (`git checkout -b blackboxai/upload-files`)
- [ ] 6. Add all files (`git add .`)
- [ ] 7. Commit changes (`git commit -m "Upload remaining Java files excluding leetcode and polymorphism"`)
- [ ] 8. Push to remote (`git push -u origin blackboxai/upload-files`)
- [ ] 9. Create Pull Request (`gh pr create --title "Upload remaining Java files" --body "Sync local Java OOP/pattern/strings/method/Inheritance files to repo. Excluded leetcode/ and polymorphism per request."`)

Current progress updated after each step.

